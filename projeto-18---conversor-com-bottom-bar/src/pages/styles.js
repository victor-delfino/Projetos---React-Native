import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: { padding: 20 },
  titulo: { fontSize: 20, textAlign: 'center', marginBottom: 20, color: 'red' },
  resultado: { fontSize: 18, textAlign: 'center', marginTop: 20, color: 'green' },
});

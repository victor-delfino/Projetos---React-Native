import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  image: {
    width: 300,
    height: 300,
    margin: 30,
  },
});

export default styles;

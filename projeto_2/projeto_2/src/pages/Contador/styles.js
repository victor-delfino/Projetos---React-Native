import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({

  titulo:{
    fontSize: 30,
    color: 'black',
    marginTop: 20,
    textAlign: 'center'
  },

  contador:{
    fontSize: 60,
    color: 'blue',
    marginTop: 20,
    textAlign: 'center'
  },

})

export {styles}
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  counter: {
    fontSize: 18,
    marginBottom: 20,
  },
  buttonContainer: {
    width: '60%',
    gap: 20,
  },
});

export default styles;
